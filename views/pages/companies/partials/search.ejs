    <div class="mt-3 mb-5 d-flex">
        <input 
            type="text" 
            class ="form-control"
            placeholder="szukaj"
            name="q" 
            value="<%-query.q%>"
        />
        <button class=""btn btn-primary ms-2> Szukaj</button>
    </div>

<% if (query.q) { %>
    <p class="h5 mb-3">Wyniki wyszukiwania dla frazy : "<%- query.q %>"</p>
<% } %>